const config = {
    host: "localhost",
    port: 3306,   //8889 for mysql at 412A, 5432 for postgres, 3389 for mysql
    user: "root",    //root
    password: "root",   //root
    database: "cinemavault",
    connection_limit:100
}


export default config;
